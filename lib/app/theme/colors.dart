import 'package:flutter/material.dart';

abstract class AppColors {
  /// Primary color
  static const pink = Color.fromRGBO(224, 144, 144, 1);

  /// On Primary & Surface & background
  static const white = Color.fromRGBO(255, 255, 255, 1);

  /// On Surface
  static const darkerPink = Color.fromRGBO(191, 121, 100, 1);

  ///  Text Color
  static const grey = Color.fromRGBO(128, 132, 143, 1);
}
